# 屏東基督教醫院 AI 應用展示

## 開啟方式
1. 將 ZIP 完整解壓縮。
2. 進入 `pch_ai_showcase_fixed` 資料夾。
3. 用 Chrome、Edge 或 Safari 開啟 `index.html`。
4. 不要只把 `index.html` 單獨移出資料夾，`assets` 必須一起保留。

此版本包含完整 CSS、JavaScript、操作圖片、HIS 與 AI 整合架構圖，以及 ChatPTCH 示範影片。
